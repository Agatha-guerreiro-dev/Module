from useful.data import validation
num_int = validation.readint('Type a number: ')
num_float = validation.readfloat('Type a number: ')
print(f'{num_int} is an integer number and {num_float} is a floating point number!')
